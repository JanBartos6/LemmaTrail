# TOPP Import

This directory contains metadata imported from the public
`edemaine/topp` source repository.

The import is metadata-only. Problem statements and status prose are not
copied here.

Use `problems.yaml` directly as the catalog listing.

Importer code exists for maintainers, but normal users do not need to run it.
