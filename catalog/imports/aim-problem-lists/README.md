# AIM Problem Lists Import

This directory contains link metadata imported from the public AIM Problem
Lists index.

The import is a list index only. AIMPL pages, PDFs, and problem statements are
not crawled or copied here.

Use `problem-lists.yaml` directly as the catalog listing.

Importer code exists for maintainers, but normal users do not need to run it.
