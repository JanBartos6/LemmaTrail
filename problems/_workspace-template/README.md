# Problem Workspace Template

Copy this directory to start a new active problem workspace:

```text
problems/<problem-id>/
```

After copying, replace every `TEMPLATE` placeholder with the stable problem ID.

This directory is a workspace skeleton, not research state. The root
`templates/` directory contains templates for individual claims, routes,
attempts, failures, and reviews.
